# Tiny Tapeout project information
project:
  title: "4-bit Neural Network Accelerator" # Project title
  author: "timasangwan-amit" # Your name
  discord: "" # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description: "A 4-bit neural network MAC accelerator with ReLU activation, supporting LOAD_BIAS, MAC, and ACTIVATE operations via a 2-bit opcode interface." # One line description of what your project does
  language: "Verilog" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz: 1000000 # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 340x160 uM.
  tiles: "1x1" # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 3x4 or 4x4

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module: "tt_um_four_bit_nn_accel"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "project.v"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "shift_val[0]"
  ui[1]: "shift_val[1]"
  ui[2]: "shift_val[2]"
  ui[3]: ""
  ui[4]: ""
  ui[5]: ""
  ui[6]: "op_code[0]"
  ui[7]: "op_code[1]"

  # Outputs
  uo[0]: "result[0]"
  uo[1]: "result[1]"
  uo[2]: "result[2]"
  uo[3]: "result[3]"
  uo[4]: ""
  uo[5]: ""
  uo[6]: ""
  uo[7]: ""

  # Bidirectional pins
  uio[0]: "feature[0]"
  uio[1]: "feature[1]"
  uio[2]: "feature[2]"
  uio[3]: "feature[3]"
  uio[4]: "weight[0]"
  uio[5]: "weight[1]"
  uio[6]: "weight[2]"
  uio[7]: "weight[3]"

# Do not change!
yaml_version: 6
